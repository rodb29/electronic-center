const nav=document.querySelector('.nav');const toggle=document.querySelector('.menu-toggle');if(toggle)toggle.addEventListener('click',()=>nav.classList.toggle('open'));
const WHATSAPP_NUMBER='';
const form=document.querySelector('#contact-form');if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);if(!WHATSAPP_NUMBER){document.querySelector('#form-status').innerHTML='<div class="notice">Recibimos tu solicitud. El número oficial de WhatsApp se conectará cuando esté definido; por ahora puedes guardar esta solicitud y contactarnos por el canal que indiquemos.</div>';form.reset();return;}const msg=`Hola, soy ${data.get('name')}. Necesito información sobre ${data.get('service')}. ${data.get('message')}`;const url=`https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(msg)}`;document.querySelector('#form-status').innerHTML=`<div class="notice">Recibimos tu solicitud.</div><div class="actions"><a class="btn btn-primary" href="${url}" target="_blank" rel="noopener">Continuar por WhatsApp</a></div>`;form.reset()})}